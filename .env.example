# Copy to .env (gitignored) and fill in real values before running
# `docker compose up -d`. docker-compose.yml substitutes these into the
# api service's environment.
JWT_SECRET_KEY=
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
CLOUDINARY_BASE_URL=
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=
RESEND_API_KEY=
RESEND_FROM_EMAIL=
